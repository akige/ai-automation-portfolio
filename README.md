# AI Automation Engineering — portfolio site

Static one-pager, deployed via GitHub Pages.

Before going live for real clients, replace in `index.html`:
- `CONTACT_EMAIL_PLACEHOLDER` → `mailto:<your-public-email>`
- `UPWORK_PROFILE_PLACEHOLDER` → Upwork profile URL
